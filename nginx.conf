upstream webapp {
    # Using environment variables generated by Tye
    server ${WEBAPP_SERVICE_HOST}:${WEBAPP_SERVICE_PORT};
}

upstream appservice {
    # Using environment variables generated by Tye
    server ${APPSERVICE_SERVICE_HOST}:${APPSERVICE_SERVICE_PORT};
}

upstream apikeys {
    # Using environment variables generated by Tye
    server ${APIKEYS_SERVICE_HOST}:${APIKEYS_SERVICE_PORT};
}

upstream customers {
    # Using environment variables generated by Tye
    server ${CUSTOMERS_SERVICE_HOST}:${CUSTOMERS_SERVICE_PORT};
}

upstream azurite {
    # Using environment variables generated by Tye
    server ${AZURITE_BLOB_SERVICE_HOST}:${AZURITE_BLOB_SERVICE_PORT};
}

upstream identity {
    # Using environment variables generated by Tye
    server ${IDENTITY_SERVICE_HOST}:${IDENTITY_SERVICE_PORT};
}

upstream timereport {
    # Using environment variables generated by Tye
    server ${TIMEREPORT_SERVICE_HOST}:${TIMEREPORT_SERVICE_PORT};
}

upstream showroom {
    # Using environment variables generated by Tye
    server ${SHOWROOM_SERVICE_HOST}:${SHOWROOM_SERVICE_PORT};
}

upstream accounting {
    # Using environment variables generated by Tye
    server ${ACCOUNTING_SERVICE_HOST}:${ACCOUNTING_SERVICE_PORT};
}

upstream transactions {
    # Using environment variables generated by Tye
    server ${TRANSACTIONS_SERVICE_HOST}:${TRANSACTIONS_SERVICE_PORT};
}

upstream payments {
    # Using environment variables generated by Tye
    server ${PAYMENTS_SERVICE_HOST}:${PAYMENTS_SERVICE_PORT};
}

upstream invoicing {
    # Using environment variables generated by Tye
    server ${INVOICES_SERVICE_HOST}:${INVOICES_SERVICE_PORT};
}

upstream documents {
    # Using environment variables generated by Tye
    server ${DOCUMENTS_SERVICE_HOST}:${DOCUMENTS_SERVICE_PORT};
}

upstream messenger {
    # Using environment variables generated by Tye
    server ${MESSENGER_SERVICE_HOST}:${MESSENGER_SERVICE_PORT};
}

server {
    listen 80 default_server;
    server_name localhost;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name localhost;

    ssl_certificate /etc/nginx/certs/localhost.crt;
    ssl_certificate_key /etc/nginx/certs/localhost.key;

    location / {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://webapp/;

        resolver 127.0.0.11;
    }

    location /api/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://appservice/;

        proxy_connect_timeout       600;
        proxy_send_timeout          600;
        proxy_read_timeout          600;
        send_timeout                600;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/apikeys/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://apikeys/;

        proxy_connect_timeout       600;
        proxy_send_timeout          600;
        proxy_read_timeout          600;
        send_timeout                600;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/customers/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://customers/;

        proxy_connect_timeout       600;
        proxy_send_timeout          600;
        proxy_read_timeout          600;
        send_timeout                600;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/timereport/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://timereport/;

        proxy_connect_timeout       600;
        proxy_send_timeout          600;
        proxy_read_timeout          600;
        send_timeout                600;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/showroom/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://showroom/;

        proxy_connect_timeout       600;
        proxy_send_timeout          600;
        proxy_read_timeout          600;
        send_timeout                600;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }


     location /api/accounting/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://accounting/;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/transactions/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://transactions/;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/payments/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://payments/;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/invoicing/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://invoicing/;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/documents/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://documents/;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /api/messenger/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://messenger/;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }

    location /content/ {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass http://azurite/devstoreaccount1/;

        resolver 127.0.0.11;

        client_max_body_size 3M;
    }
}

server {
    listen 80;
    server_name identity.local;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name identity.local;

    ssl_certificate /etc/nginx/certs/server.crt;
    ssl_certificate_key /etc/nginx/certs/server.key;

    location / {
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header   Connection keep-alive;
        proxy_set_header   Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_pass https://identity/;

        resolver 127.0.0.11;
    }
}